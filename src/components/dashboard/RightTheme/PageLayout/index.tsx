import React from "react";
import "./index.scss";
const prefixCls = "right-theme-page-layout";
/** 右侧主题的页面布局设置 */
const PageLayout: React.FC = () => {
  return <div className={`${prefixCls}-container`}> PageLayout</div>;
};

export default PageLayout;
