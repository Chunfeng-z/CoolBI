import React from "react";

const prefixCls = "action-panel";
/** 仪表板操作栏 */
const ActionPanel: React.FC = () => {
  return <div className={`${prefixCls}-container`}>ActionPanel</div>;
};

export default ActionPanel;
