import React from "react";
import "./index.scss";
const prefixCls = "component-word-config";
/** 组件字段配置 */
const ComponentWordConfig: React.FC = () => {
  return <div className={`${prefixCls}-container`}>ComponentWordConfig</div>;
};

export default ComponentWordConfig;
