import React from "react";

const prefixCls = "data-label";
/** 图表配置-数据标签 */
const DataLabel: React.FC = () => {
  return <div className={`${prefixCls}-container`}>DataLabel</div>;
};

export default DataLabel;
