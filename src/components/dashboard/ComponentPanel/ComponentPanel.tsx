import React from "react";

const prefixCls = "component-panel";
/** 仪表板-组件操作菜单 */
const ComponentPanel: React.FC = () => {
  return <div className={`${prefixCls}-container`}>ComponentPanel</div>;
};

export default ComponentPanel;
